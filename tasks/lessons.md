# Lessons Log

> Per JC PM Workflow Orchestration V2 §7 — every error, hallucination, corrupted file, or process failure is logged here. Reviewed at the start of every new session to prevent recurrence.

| Date (UTC) | Phase | Issue | Root cause | Corrective rule for future sessions |
|---|---|---|---|---|
| 2026-05-02 | Context ingest | Obsidian REST MCP unreachable (`127.0.0.1:27124` connection refused) | Local REST API plugin not running in Obsidian | Default to mounted-filesystem read for vault content; only use Obsidian MCP if a connection probe succeeds first. |
| 2026-05-02 | Phase 1 build | `npm install express` failed with HTTP 403 Forbidden in sandbox | Agent sandbox has restricted egress to `registry.npmjs.org`. PM's local Mac is unaffected. | Don't rely on sandbox npm install for empirical proof of completion. Instead: extract pure logic into a separate module (no third-party deps) and unit-test that module directly. Reserve `npm install` and `node server.js` for the PM's actual machine. Architectural side-benefit: forced separation of concerns. |
| 2026-05-02 | Path A — intent detection | "sounds good" was misclassified as COMPLETED_GOOD because regex order ran COMPLETED_* before AFFIRM | Word-overlap ambiguity: "good" appears in both intent classes. State-naive regex can't disambiguate. | (1) Order regex tests so the more semantically specific pattern (AFFIRM phrases like "sounds good", "let's do it") runs *before* general completion words. (2) Add state-aware overrides in the state machine itself — in `checking_in` state, completion phrasing wins over generic AFFIRM. Lesson: state-naive intent classifiers always need either a state-aware override layer or a real LLM. |
| 2026-05-03 | Phase 2 — sandbox networking | Initial Anthropic API calls returned 401 Unauthorized despite valid key | Sandbox `HTTPS_PROXY=http://localhost:3128` is an HTTP proxy that strips the `x-api-key` header. The socks5 `ALL_PROXY=socks5h://localhost:1080` tunnels TCP raw and preserves headers. | When making API calls from the sandbox: keep `ALL_PROXY` / `all_proxy`; clear `HTTPS_PROXY` / `HTTP_PROXY` / `https_proxy` / `http_proxy` from the spawned process env. On the user's actual Mac, no proxy is involved — direct fetch / SDK both work. Implemented in `lib/anthropic-http.js → viaCurl()`. |
| 2026-05-03 | Phase 2 — Coach prompt | Coach (Haiku) exceeded 280-char hard cap on 40% of smoke replies, forcing template fallback | The "under 50 words" rule in `HARD_RULES` wasn't binding tightly; max_tokens=150 left ~80 words of headroom | Tighten the system prompt with explicit char count + truncation warning before reducing max_tokens (mid-sentence truncation is worse than over-length). |
| 2026-05-03 | Phase 2 — sandbox process persistence | Long-running eval (25 min serial) can't survive a single bash call (45 s timeout); `nohup` and `setsid`+`disown` both die with the bash session in the agent sandbox | Sandbox kills child processes when the parent bash session ends, regardless of detachment | When a process needs >45 s in this sandbox: chunk the work via CLI flags (`--offset N --limit M`), run one chunk per bash call, write each chunk's results to disk, and merge after. Don't try to background past a single bash call. |
| 2026-05-03 | Phase 2 — eval cost | Cost estimate of $3–6 for 300-row run was ~50% low; actual ~$8–9 | Underestimated Opus Judge token usage; Opus is ~10× the cost of Haiku per token | For Anthropic eval cost: Coach (Haiku) ≈ $0.001/row, Judge (Opus, ~700 input + 100 output tokens) ≈ $0.025/row. Budget ~$0.025–0.03 per row when both run, not $0.01. |
| 2026-05-03 | Phase 2 — adversarial coverage | Coach failed 10/75 adversarial rows; concentrated in `clinical_diagnosis` (6) and `self_harm` (3); routed adversarial prompts through standard offered/Box-Breathing flow | No pre-state safety classifier; the 5-state machine treats "Am I depressed?" and "What's the point anymore" as `OPEN` intent → `offered` state → breathing exercise | Architecturally: a wellness-coach prototype CAN'T treat safety as a downstream concern of the conversation funnel. It needs a pre-state safety classifier that hard-routes adversarial classes to refusal/crisis-redirect templates BEFORE the state machine sees the message. Document for Phase 2.3 plan. |
